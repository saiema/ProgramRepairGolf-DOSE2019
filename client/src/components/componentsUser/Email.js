import React from 'react'

import Reset from './ResetPassword';
const Email = ({resPass }) => {
    return (
      <div >
        <Reset resPass={resPass}/>
      </div>
    );
  }

  export default Email
